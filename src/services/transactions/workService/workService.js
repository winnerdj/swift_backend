"use strict";

// const { serviceService } = require('../../data-management');
const dataLayer = require('./dataLayer');

exports.getDropdownAvailableCounter = async({
	filters
}) => {
	try {
		// let filters = 

		/** Get the Counter Prefix */
		// let service = await serviceService.getService({ filters })
	}
	catch(e) {
		throw e
	}
}
